<!-- locals: { :q, :url } -->
<%= search_form_for(q, url: url) do |f| %>
	<div class="form-outline">
		<%= f.search_field :title_or_content_cont, class: "form-control bg-light", placeholder: "Search", style: "min-width:125px;" %>
	</div>
<% end %>
