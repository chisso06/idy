<% provide(:title, "Log In") %>
<% provide(:form_title, "Log In") %>

<h1>Log In</h1>

<%= form_for(:user, url: login_path) do |f| %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Log In" %>
<% end %>