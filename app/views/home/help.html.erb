<% provide("Help", :title) %>

<h1>Help</h1>

<div class="accordion mt-4" id="myAccordion">
	<% @lists.each_with_index do |list, i| %>
		<%= render partial: 'home/help_list', locals: { i: i, title: list[:title], text: list[:text] } %>
	<% end %>
</div>

